---
title: Playground
description: Convert HTML to PDF live in your browser.
---

import ConvertPlayground from "../../components/ConvertPlayground.tsx";

Edit the HTML and click **Generate PDF**. This calls `POST /convert` on the configured
API (`PUBLIC_API_URL`, default `http://localhost:8080`). The API must allow this site's
origin via `CORS_ALLOWED_ORIGINS`.

<ConvertPlayground client:only="react" />
