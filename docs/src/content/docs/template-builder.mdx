---
title: Template Builder
description: Design PDF templates visually and render them through the API.
---

import TemplateBuilderIsland from "../../components/TemplateBuilderIsland.tsx";

Design a template visually, then render it to an accessible PDF. The builder calls the
API at `PUBLIC_API_URL` (defaults to `http://localhost:8080` for local development).

<TemplateBuilderIsland client:only="react" />
