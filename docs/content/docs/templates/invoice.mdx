---
title: Invoice
description: A realistic invoice template.
---

import TemplateBuilderIsland from "../../../components/TemplateBuilderIsland.tsx";
import invoice from "../../../examples/templates/invoice.json?raw";

A complete invoice combining headings, key-value blocks, and a line-items table. Use the visual
builder to adjust the document structure and click **Render PDF** to see the result.

<TemplateBuilderIsland initialCode={invoice} client:only="react" />
